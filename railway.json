{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npx turbo run build --filter=@iadl/api"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=apps/api/prisma/schema.prisma && npm run start --workspace=@iadl/api",
    "restartPolicyType": "ON_FAILURE"
  }
}
